# Defaults for `sam deploy`. Run `sam deploy --guided` once to confirm/region.
version = 0.1

[default.global.parameters]
stack_name = "rpg-me"

[default.build.parameters]
cached = true
parallel = true

[default.deploy.parameters]
capabilities = "CAPABILITY_IAM"
confirm_changeset = true
resolve_s3 = true
